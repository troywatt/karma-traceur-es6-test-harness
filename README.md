karma-traceur-es6-test-harness
==================
Example project utilizing karma and traceur-compiler to write and test ES6 style JavaScript with mocha and mocha style reporter.  To run:

```bash
npm install
karma start
```
